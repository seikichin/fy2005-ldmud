#define X_COOR -20
#define Y_COOR -30
#define Z_COOR 320
#define R_FLAG 33
#define FREE_SPACE 3
#define CONSTRUCT_TREE 1
#define BASIC_TAX 560
#define BASIC_SCORE_TAX 300

#define TOTAL_GOLD 600
#define TOTAL_SCORE 380

#define OWNER "ckk"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1619274390"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "羽化楼");
set("long", "\n"
"铁血山庄门下羽长愁的居所是为了装一些从武林人士哪里抢来的奇珍异宝羽长愁在打宝队伍里面是很关键的人物他起到的作用就是肉盾毕竟铁血山庄的特长就是血条长长长长长长长长长长长长长长长长长长\n"
"\n"
""
);
        set("exits", ([
"out" : "/p/residence/smile8.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
